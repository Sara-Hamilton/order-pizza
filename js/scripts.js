// business logic
function Pizza {
  this.toppings = [];
  this.size = size;
}

Pizza.prototype.cost = function(toppings, size) {
  return ((toppings.length * toppingsPrice) + sizePrice))
}


// business logic
